# Cloudflare PREPENDS its managed block above this: a "User-agent: *" group
# with Allow: / and a content signal, then Disallow rules for the AI-training
# crawlers. Do not declare another "User-agent: *" group here - it would be a
# duplicate of one that already exists, and parsers differ on how they merge
# repeated groups. Sitemap is a non-group directive, so it applies wherever it
# sits in the file.

Sitemap: https://brownpigllc.com/sitemap.xml
